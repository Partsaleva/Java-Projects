package oneYearLove.source.main;

import java.awt.EventQueue;
import java.io.File;

import oneYearLove.gui.Box;
import oneYearLove.source.fileControl.FileAccess;



public class RunProgram {

	//TODO problem when runnable jar is exported
	public static void main(String[] args) {
		
		 String name = System.getProperty("user.dir")+File.separator+FileAccess.class.getPackage().getName()+File.separator+"holiday365";
         System.out.println(name);
        // FileWriter fw = new FileWriter(name);
       /*  
		EventQueue.invokeLater(new Runnable() {
			public void run() {
				try {
					Box frame = new Box();
					frame.setVisible(true);
					
				} catch (Exception e) {
					e.printStackTrace();
				}
			}
		});*/
	}

}
